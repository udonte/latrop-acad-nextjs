import React from "react";
import ResetPassword from "./components/ResetPassword";

export const metadata = {
	title: "Reset Password - Gurugeeks Academy",
};

const page = () => {
	return (
		<div>
			<ResetPassword />
		</div>
	);
};

export default page;
